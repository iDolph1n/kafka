server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/credit
    username: credit
    password: credit
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  kafka:
    bootstrap-servers: kafka:9092
  rabbitmq:
    host: rabbitmq

app:
  kafka:
    topic:
      creditApplications: credit-applications
